name: CI

on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * *'

permissions:
  contents: write
  issues: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v7

    - name: Set up Python
      uses: actions/setup-python@v7
      with:
        python-version: '3.x'

    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y ninja-build clang llvm

    - name: Install llvm-ar
      run: |
        # The 'llvm' package doesn't include llvm-ar. We need to install the
        # tools package. To do this robustly, we first find the major version of
        # the installed llvm package.
        LLVM_VERSION=$(llvm-config --version | cut -d'.' -f1)
        echo "Detected LLVM version: $LLVM_VERSION"

        # Now, install the corresponding version-specific tools package.
        sudo apt-get install -y "llvm-$LLVM_VERSION-tools"

        # Verify that llvm-ar is now available
        which llvm-ar

    - name: Install clang 21 for GN
      run: |
        # GN requires C++23 std::expected, which the runner's default clang
        # (18) can't use with libstdc++. PartitionAlloc keeps the default clang.
        wget -qO- https://apt.llvm.org/llvm.sh | sudo bash -s -- 21
        clang++-21 --version

    - name: Run the build script
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        GITHUB_REPOSITORY: ${{ github.repository }}
        # Read by GN's build/gen.py only; PartitionAlloc's toolchain hardcodes
        # clang++.
        CXX: clang++-21
      run: python main.py

    - name: Commit changes
      run: |
        git config --global user.name 'github-actions[bot]'
        git config --global user.email 'github-actions[bot]@users.noreply.github.com'
        git add README.md
        git add output/*
        git add author_map.json
        git commit -m "Update build status and author map" || echo "No changes to commit"
        git push
